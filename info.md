# Python Flask CI/CD Lab

This is a Python Flask application designed for a CI/CD lab. It demonstrates Continuous Integration and Continuous Deployment practices using GitHub Actions.

## Installing Requirements

To install the project dependencies, run:

```bash
pip install -r requirements.txt
```

## Running the Tests

To run the tests, use:

```bash
python -m pytest
```
